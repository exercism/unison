message : Text -> Text
message logLine =
  "REPLACE ME! Takes a string representing a log line and returns its message with whitespace trimmed."

logLevel : Text -> Text
logLevel logLine =
  "REPLACE ME! Takes a string representing a log line and returns its level in lower-case."


reformat : Text -> Text
reformat logLine =
  "REPLACE ME! Takes a string representing a log line and formats it with the message first and the log level in parentheses."
