raindrops.test.ex1 = verify do
  labeled "the sound for 1 is 1" do
    expected = "1"
    actual = convert 1
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex2 = verify do
  labeled "the sound for 3 is Pling" do
    expected = "Pling"
    actual = convert 3
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex3 = verify do
  labeled "the sound for 5 is Plang" do
    expected = "Plang"
    actual = convert 5
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex4 = verify do
  labeled "the sound for 7 is Plong" do
    expected = "Plong"
    actual = convert 7
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex5 = verify do
  labeled "the sound for 6 is Pling as it has a factor 3" do
    expected = "Pling"
    actual = convert 6
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex6 = verify do
  labeled "2 to the power 3 does not make a raindrop sound as 3 is the exponent not the base" do
    expected = "8"
    actual = convert 8
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex7 = verify do
  labeled "the sound for 9 is Pling as it has a factor 3" do
    expected = "Pling"
    actual = convert 9
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex8 = verify do
  labeled "the sound for 10 is Plang as it has a factor 5" do
    expected = "Plang"
    actual = convert 10
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex9 = verify do
  labeled "the sound for 14 is Plong as it has a factor of 7" do
    expected = "Plong"
    actual = convert 14
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex10 = verify do
  labeled "the sound for 15 is PlingPlang as it has factors 3 and 5" do
    expected = "PlingPlang"
    actual = convert 15
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex11 = verify do
  labeled "the sound for 21 is PlingPlong as it has factors 3 and 7" do
    expected = "PlingPlong"
    actual = convert 21
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex12 = verify do
  labeled "the sound for 25 is Plang as it has a factor 5" do
    expected = "Plang"
    actual = convert 25
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex13 = verify do
  labeled "the sound for 27 is Pling as it has a factor 3" do
    expected = "Pling"
    actual = convert 27
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex14 = verify do
  labeled "the sound for 35 is PlangPlong as it has factors 5 and 7" do
    expected = "PlangPlong"
    actual = convert 35
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex15 = verify do
  labeled "the sound for 49 is Plong as it has a factor 7" do
    expected = "Plong"
    actual = convert 49
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex16 = verify do
  labeled "the sound for 52 is 52" do
    expected = "52"
    actual = convert 52
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex17 = verify do
  labeled "the sound for 105 is PlingPlangPlong as it has factors 3, 5 and 7" do
    expected = "PlingPlangPlong"
    actual = convert 105
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual

raindrops.test.ex18 = verify do
  labeled "the sound for 3125 is Plang as it has a factor 5" do
    expected = "Plang"
    actual = convert 3125
    label "expected" expected
    label "actual" actual
    ensureEqual expected actual


test> raindrops.tests = join [
  raindrops.test.ex1,
  raindrops.test.ex2,
  raindrops.test.ex3,
  raindrops.test.ex4,
  raindrops.test.ex5,
  raindrops.test.ex6,
  raindrops.test.ex7,
  raindrops.test.ex8,
  raindrops.test.ex9,
  raindrops.test.ex10,
  raindrops.test.ex11,
  raindrops.test.ex12,
  raindrops.test.ex13,
  raindrops.test.ex14,
  raindrops.test.ex15,
  raindrops.test.ex16,
  raindrops.test.ex17,
  raindrops.test.ex18
]
